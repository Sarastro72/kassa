services:
    app:
        container_name: kassa
        image: sarastro72/kassa:test
        pull_policy: build
        build:
            context: .
            dockerfile: Dockerfile
        restart: always
        ports:
            - "5555:8080"
